; ============================================================================
;  图书馆借阅管理系统 — 运行配置
;
;  程序启动时按以下顺序查找本文件：
;    1. 可执行文件所在目录的 config.ini；
;    2. 自可执行文件目录向上最多 4 层的 config.ini（便于在 build/ 里直接运行）。
;
;  也支持用环境变量临时覆盖，便于连接测试库而无需改动本文件：
;    LIBMS_DB_HOST / LIBMS_DB_PORT / LIBMS_DB_USER / LIBMS_DB_PASSWORD / LIBMS_DB_NAME
; ============================================================================

[Database]
HostName=127.0.0.1
Port=3306
UserName=root
Password=123456
DatabaseName=book_system
ConnectTimeout=5
